import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'apulia.lastminute.app',
  appName: 'apulialastminute',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
